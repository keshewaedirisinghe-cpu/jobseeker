# Operations

Use `make up`, `make check`, `scripts/backup.sh`, and `scripts/restore.sh`. The Windows PC must remain powered on with Docker Desktop running for continuous operation. Restore should be tested into a fresh checkout before relying on backups.
